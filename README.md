# A1 AOS
 A repository for the first assignment of the module.

FYI, I used Visual Studio and Ubuntu WSL.

INSTRUCTIONS for executing the task 1 bash file system_manager.sh:
-Download the Task 1 bash file and place into the desired directory, make sure VScode knows what directory the file is in.
-'chmod +x system_manager.sh' (this makes the script executable)
-'./system_manager.sh' (this runs the script - make sure the directory is correct otherwise won't work)
-Main menu should display in terminal, select options 1-6.
